# CLAUDE.md — ذاكرة مشروع fiqhpedia

> ارفع هذا الملف في أول رسالة من أي محادثة جديدة. يختصر السياق كله.
> **حدّثه كلما تغيّرت البنية أو أُضيف نوع محتوى جديد.**

## ما هو المشروع

موسوعة فقهية ثلاثية اللغة (عربي / إنجليزي / روسي) تعرض حكم كل مذهب من
المذاهب الأربعة في كل مسألة، مع مراجعه.

**الهدف: أن يجد كل شخص مذهبه — لا المقارنة بينها للترجيح.** هذا فرق
جوهري وليس لفظياً: تجنّب كلمة «مقارن» في هوية الموقع. تبقى فقط في اسم
أداة «المقارنة الثنائية» لأنها تصف وظيفة حقيقية.

- الموقع: https://fiqhpedia.pages.dev
- الريبو: https://github.com/AkhBusiness/fiqhpedia
- الاستضافة: Cloudflare Pages، نشر تلقائي عند كل `git push`
- المالك يعمل وحده، ولا يريد فريق محررين.

## التقنية

Next.js 16 مع `output: 'export'` — موقع ثابت بالكامل. pnpm. Tailwind v4.
لا قاعدة بيانات: كل المحتوى في `data/fiqhData.json`، وتفضيلات الزائر في
`localStorage`.

**لا تقترح قاعدة بيانات للمحتوى.** المحتوى مرجعي قليل التغيّر، والملفات
داخل Git تعطي تاريخ تعديلات كاملاً وصفر تكلفة.

## بنية البيانات — `data/fiqhData.json`

| المفتاح | العدد | الوصف |
|---|---|---|
| `issues` | 19 | المسائل الفقهية |
| `theology` | 11 | الأدلة العقلية |
| `articles` | 1 | المقالات المطوّلة |
| `countries` | 27 | الدولة ← المذهب |
| `books` | 9 | أبواب الفقه |
| `guides` | 3 | أدلة عملية بخطوات |
| `faqs` | 6 | أسئلة شائعة |
| `glossary` | 8 | مصطلحات |
| `ui` | 94 | نصوص الواجهة |

### المسألة

```
id, ref, bookId, number, chapter?, title, summary,
rulings: { hanafi|maliki|shafii|hanbali: { text, sources[] } }
```

كل حقل نصي كائن `{ar, en, ru}`. `sources` **قائمة** كتب لا نصاً واحداً.
`number` يعيد العدّ داخل كل باب، فهو **ليس** فريداً — الفريد هو `ref`.

### الدليل العقلي

```
id, ref, title, tagline, premises[], quran{verse, ref}, conclusion
```

### المقالة

```
id, ref, title, excerpt, relatedRefs[], sections[{id, heading, body}]
```

`heading` فارغ في القسم الافتتاحي. في `body`: السطر الفارغ يفصل فقرة،
و`**نص**` يجعله عريضاً. `relatedRefs` تشير إلى أرقام أدلة (`A9`...).

## الأرقام المرجعية — قاعدة لا تُخرق

`F1…` مسائل · `A1…` أدلة · `M1…` مقالات

**الرقم دائم.** بعد نشره لا يُغيَّر ولا يُعاد استعماله لعنصر آخر أبداً،
لأن الناس يشاركونه ويحفظونه. لو حُذف عنصر، يبقى رقمه شاغراً للأبد.

الروابط المباشرة تعمل: `/#A3` و `/#F12` و `/#M1` تفتح العنصر مباشرة
وتنتقل لتبويبه.

## سير العمل — لا تخالفه

1. **اسحب آخر نسخة من GitHub قبل أي تعديل.** حصل مرة أن دُمج على نسخة
   قديمة فتكررت المسائل وفشل النشر.
2. عدّل، ثم `python validate_content.py` — يجب `PASSED — 0 errors`.
3. `npx tsc --noEmit` ثم `npx next build` عند تغيير الكود.
4. **قارن كل ملف** مقابل النسخة الحية قبل التسليم، لا الملفات التي تظنها
   تغيّرت فقط. حصل مرة أن نقصت ثلاثة ملفات فانكسرت الأنواع.
5. سلّم **أرشيف zip** فيه بنية المجلدات جاهزة، لا ملفات منفصلة.

المالك على **ويندوز / PowerShell** — لا تعطه أوامر لينكس مثل `printf`.

## المدقّق — `validate_content.py`

يفحص: الترجمات الثلاث لكل حقل · المذاهب الأربعة كاملة · الأحرف التالفة
(U+FFFD) · النص الملصوق بلغة خاطئة · المعرّفات والأرقام المكررة · صيغة
`ref` · قوائم المراجع (فارغة، مكررة، كتابان في خانة) · أقواس الآيات
وعلامة القطع · روابط المقالات المعلّقة · بيانات الدول.

**يفحص وجود الترجمة لا صحتها.** الترجمة الخاطئة تمرّ.

نصيحة: `echo 'python validate_content.py || exit 1' > .git/hooks/pre-commit`

## قواعد المحتوى

**اللغات الثلاث معاً في كل تعديل.** تصحيح العربي وحده يترك الترجمتين على
المعنى القديم. وقع فعلاً: صُحّح «واجب الوجود» بالعربي وبقي
`Necessary Being` بالإنجليزي.

**كل حقل بأبجدية لغته وحدها.** تسرّب حرف عربي داخل كلمة روسية خطأ متكرر
ولا يُرى بالعين. وقع ثلاث مرات.

**المراجع بأسماء الكتب مجردة** بلا مؤلف: «الرد المحتار» لا «رد المحتار
لابن عابدين». القائمة المعتمدة في `data/terms.json` وهي **مغلقة**،
يحرسها المدقّق: كتاب خارجها أو منسوب لغير مذهبه خطأ يُرفض.

**الأدلة العقلية كلامية لا فلسفية.** لا تخلط مصطلحات دليل الإمكان
(«واجب الوجود») في دليل الحدوث الذي ينتهي إلى «مُحدِث قديم». الخلط وقع
وصُحّح. وعنوان `ui.conclusion` **«الخلاصة العقلية»** — حُسم، ولا يُعاد
فتحه. اختير لاتساقه مع «أدلة عقلية» المستعملة في الموقع؛ ورُفضت
«البرهانية» لأن «البرهان» مصطلح منطق أرسطو نفسه، ولثقل ترجمتها.

**الآيات**: إما تامة، أو ينتهي الاقتباس بـ`...` داخل ﴿﴾ ليعلم القارئ أنه
شطر. علامة القطع متسقة عبر اللغات الثلاث.

**راجع صحة المحتوى الشرعي مع مختصّ قبل النشر.** النماذج تخطئ في نسبة
الأقوال للمذاهب بصياغة واثقة.

## سير المحتوى الجديد

المالك يولّد المحتوى بـ Gemini ثم يعطيه لكلود ليحوّله ويدمجه.

`prompts/_core.md` في رأس كل جلسة، ثم `prompts/_terms.md`، ثم ملف القسم:
`issues.md` · `theology.md`. البرومبتات في المستودع ومتعقّبة.

جمناي **لا يولّد** `id` ولا `ref` ولا `number` ولا `bookId`. يسندها المستودع.

ذيل البرومبت مولَّد: `python3 tools/list_existing.py theology|issues <باب>`.

اطلب **٥ مسائل** لا ١٠ — الالتزام أفضل مع الأعداد الأصغر (طُلب ١٠ فسُلّم ٣).

**كلود هو من يترجم**، لا Gemini: توحيد المصطلحات مع بقية الموقع، وتفادي
تسرّب الحروف.

## معلّقات

- **«وجه الدلالة»** — اقترحه المدقّق ثلاث مرات: شرح كيف تدلّ الآية على
  المطلوب. يحتاج حقلاً جديداً + مكوّن عرض + كتابته للأدلة الـ11 كلها.
- **ملخصات `salah-1..3`** جمل خبرية بطول 18 كلمة بدل أسئلة قصيرة.
- **ستة أبواب فارغة**: الزكاة، الصوم، الحج، المعاملات، النكاح، الجنايات.
- **لوحة تحكم** من نوع Git-backed (Sveltia / Decap) — أُجّلت.
- **بحث Pagefind** — أُجّل.

## أسلوب التعامل

- اسأل قبل التنفيذ إن كان القرار يخصّ المحتوى أو الهوية.
- لا تحوّل شيئاً إلى شكل آخر دون إذن — حصل مع مقالة حُوّلت لأدلة.
- تحقّق بالفحص لا بالذاكرة: اقرأ الملف، شغّل الأمر، اعرض الناتج.

# CLAUDE.md — ذاكرة مشروع تبيان

> ارفع هذا الملف في أول رسالة من أي محادثة جديدة، ومعه:
> `القواعد-العامة.md` · `ضوابط-المذاهب-الأربعة.md` · `خريطة-الأبواب.md`
>
> **حدّثه كلما تغيّرت البنية أو أُضيف نوع محتوى.**

## ما هو المشروع

**تبيان** — موسوعة إسلامية بخمس لغات، تعرض حكم كل مذهب من المذاهب الأربعة في
كل مسألة بمراجعه، ومعها مقالات وأدلة عقلية ومفردات.

**الهدف: أن يجد كل شخص مذهبه — لا المقارنة بينها للترجيح.** فرقٌ جوهري لا
لفظي: تُجتنب كلمة «مقارن» في هوية الموقع.

النبذة المعتمدة: «الإسلام من منابعه — فقهاً وعقيدةً وسيرة».

- الموقع: https://fiqhpedia.pages.dev — النطاق قديم، والاسم صار «تبيان»
- الريبو: https://github.com/AkhBusiness/fiqhpedia
- الاستضافة: Cloudflare Pages، نشر تلقائي عند كل `git push`
- وسم **تجريبي** ظاهر بجوار الاسم
- المالك يعمل وحده، ولا يريد فريق محررين

## اللغات

| | الحالة |
|---|---|
| `ar` `en` `ru` `es` `uk` | **كلها مُطلقة** — مفروضة في المدقّق منذ 2026-09-05 |

`PENDING_LANGS` فارغة في `validate_content.py` و`lib/fiqh-data.ts` و`app/admin/page.tsx`
معاً. فأيّ حقل ناقص في أي لغة **يوقف الرفع** الآن، ولا يمرّ تنبيهاً.

الخمس مكتملة ومدقّقة بشرياً: الإسبانية والأوكرانية والروسية والإنجليزية مرّت على
مدقّق ناطق، والعربية على مراجع شرعي. **وما زال بعضها ينتظر جواباً** — انظر
`مراجعة-شرعية-ط٢.md`.

الأوكرانية طلبها جمهور فعلي، فهي مقدَّمة على الإسبانية.

## التقنية

Next.js 16 مع `output: 'export'` — موقع ثابت بالكامل. pnpm. Tailwind v4.
لا قاعدة بيانات: المحتوى في ملفات، وتفضيلات الزائر في `localStorage`.

**`.npmrc` في الجذر يحوي `enable-pre-post-scripts=true` — لا تحذفه.** بدونه
`pnpm build` لا يُشغّل `prebuild` تلقائياً (خلافاً لـ npm)، فيبني Cloudflare
الموقع من `fiqhData.json` القديم المحفوظ في آخر كوميت مهما حُدِّثت
`data/content/`. وقع هذا فعلاً ونشر الموقع نسخة قديمة رغم دفع محتوى جديد.

**لا تقترح قاعدة بيانات للمحتوى.** المحتوى مرجعي قليل التغيّر، والملفات داخل
Git تعطي تاريخ تعديلات كاملاً وصفر تكلفة.

## بنية البيانات

`data/fiqhData.json` **مولَّد ولا يُحرَّر باليد.** يُجمّع بـ:

```
python3 tools/build_data.py
```

والتحرير في `data/content/`:

| | |
|---|---|
| `core.json` | ui · books · schools · languages · theology · glossary · guides · faqs · countries |
| `chapters.json` | ترتيب فصول كل باب — مصدر الترتيب الوحيد |
| `issues/F20.json` | ملف لكل مسألة |
| `articles/M1.json` | ملف لكل مقالة |

### الأعداد الحالية

> **آخر تحديث: 2026-09-18.** العدد يتغيّر مع كل دفعة؛ تحقّق منه بـ
> `python3 tools/build_data.py` فهو يطبع المسائل والمقالات.

`issues` **115** منشورة (+ `F3` مسودّة محجوبة) — الطهارة 61 · الصلاة 31 ·
الصيام 11 · النكاح 9 · الإيمان 3 ·
`theology` 11 (في ثلاثة أبواب: `ilahiyyat` · `nubuwwat` · `quran_sila`) ·
`articles` **8** (في خمسة أبواب، انظر «أبواب المقالات» أدناه) ·
`ui` ~190 · `countries` 27 · `books` 9 · `glossary` **47**
(٨ بالبنية القديمة + ٣٩ بالجديدة: ١٨ فقهية و٢١ لمفردات المقالات) ·
`gradeLabels` 7 · `natureLabels` 4 ·
`faqs` 6 · `guides` 3 · `schools` 4 · `languages` 5

**الأحكام المُسندة:** 293 درجة (`grade`) و72 تكييفاً (`nature`) من 440 حكماً.
والباقي بلا درجة **عمداً** — مسائل التوقيت والمقادير والتعريفات لا حكم تكليفي
لها، فشارةٌ عليها أسوأ من غيابها.

### أبواب المقالات

خمسة أبواب، مرتّبة على **تدرّج ما تفترضه في القارئ** — لا بالترقيم:

| الباب | المقالات |
|---|---|
| مسائل الوجود الكبرى | `M4` دليل الخلق · `M1` رحلة البحث |
| معرفة الله وصفاته | `M8` الأسماء الحسنى |
| تاريخ الأديان ونصوصها | `M3` تغيّر التوراة والإنجيل · `M6` تفرّق النصارى · `M7` حفظ القرآن |
| الخلق والكون | `M2` بدء الخلق |
| حِكَم التشريع | `M5` الحكمة من الصيام |

الترتيب مثبَّت في `lib/fiqh-data.ts → PREFERRED`، والموضع داخل الباب بحقل
`seq` في ملف المقالة. و**الأسماء تصف الموضوع لا الموقف**: كانت «العقيدة
والتوحيد» و«نقد الأديان»، وهما يفترضان خلفيةً في القارئ والثاني تصادميّ —
ومن يرى باباً اسمه نقد دينه ينصرف قبل أن يقرأ.


### المسألة

```
ref, id, bookId, seq, chapter, title, summary, draft?,
rulings: { hanafi|maliki|shafii|hanbali: { text, sources[] } }
```

`draft: true` تُبقي الملف كاملاً في `data/content/issues/` لكن `build_data.py`
يستثنيه من `fiqhData.json` — يختفي عن الموقع دون حذف أي شيء. يُستعمل لمادة
تنتظر مراجعاً مختصّاً (مثال: `F3`، تارك الصلاة — بابا التكفير والردّة حسّاس).

كل حقل نصي كائن `{ar, en, ru, es, uk}`. و`sources` **قائمة** كتب لا نصاً.

**`number` لا يُكتب** — يُحسب عند البناء من ترتيب الفصل في `chapters.json` ثم
`seq`. و`seq` هو موضع المسألة في **خطة الفصل** في `خريطة-الأبواب.md`، ثابت لا
يتغيّر بإدراج غيره. فإدراج المسألة الخامسة لا يزحزح رقم الثامنة.

### `grade` — درجة الحكم التكليفي

حقل **اختياري** داخل كل مذهب، إلى جانب `text` و`sources`:

```json
"hanafi": { "grade": "sunnah", "text": {…}, "sources": [{…}] }
```

**حقلان لا حقل واحد** — قرّره المراجع الشرعي 2026-09-06:

- `grade` تكليفي، سبع قيم: `fard` · `wajib` · `sunnah` · `mandub` · `makruh` ·
  `haram` · `mubah`
- `nature` وضعي، أربع قيم: `rukn` · `shart` · `sabab` · `mani`

والمدقّق يرفض ما عداها. **وسبب الفصل:** غسل الوجه فرضٌ وركنٌ معاً، وحشرهما في
حقل واحد كان يُظهر الحنابلة مخالفين للثلاثة في مسألة هم متّفقون فيها.
وأسماؤهما في `gradeLabels` و`natureLabels` **منفصلتين** — مشاركة خريطة واحدة
هي الطريق الذي يعودان به إلى الاختلاط عند العرض بعد أن فُصلا في البيانات.

**الوحدة هي المسألة لا الحكم المفرد:** إمّا تُعلَّم المذاهب الأربعة معاً وإمّا
تُترك معاً. فتفاوتها في الشارة مصدره غالباً اللفظ لا الحكم — «فرض» عند اثنين
و«يجب» عند اثنين حكمٌ واحد بلفظين.

**يُترك غائباً** فيما لا حكم تكليفي له — متى يبدأ وقت العصر، وكم القلّتان،
وما تعريف المقيم. وهذه ٤١٪ من الأحكام، فالغياب هو الأصل لا الاستثناء، والواجهة
لا تعرض شارة حينئذ.

**لا تُسنَد آلياً بمطابقة ألفاظ العربي.** جُرِّب، وأخطاؤه منهجية: يلتقط لفظ
الشقّ الثانوي («…ويكره») بدل الحكم الأصلي، ويقرأ «صلاة **الفرض**» حكماً وهي
وصف للصلاة. المسوّدة الآلية للفرز وحده (`/home/claude/propose_grades.py` نموذجاً)،
والإسناد بمراجعة بشرية.

**ولا تُدمج `wajib` في `fard` عند الحنابلة.** هم كالجمهور في الأصول لا يفرّقون،
لكنهم فرّقوا في فروع بعينها: التسمية في الوضوء واجبة **تسقط بالسهو**، والفرض
لا يسقط. فما كان واجباً يسقط بالسهو يُعلَّم `wajib`.

أسماء الدرجات بالخمس لغات في `core.json → gradeLabels`. وتعريف كل درجة **عند كل
مذهب** يُكتب في `glossary` بالبنية الموسّعة، فالمصطلح الواحد يختلف معناه بالمذهب.

### `addedAt` — تاريخ الإضافة ووسم «جديد»

حقل ISO يُكتب **في ملف المسألة** بعد `ref`. و`build_data.py` يحترمه إن وُجد،
ويستنتجه من `git log` إن غاب، ويستثني ما دخل في الكوميت المؤسِّس (مادة
مستوردة لا إضافة يومية).

**لماذا يُكتب في الملف لا يُستنتج فقط:** الملفات تُبنى في بيئة لا تُودَع فيها،
فكان `git log` لا يجدها ويضع تاريخ اليوم على الدفعة كلّها. وقع فعلاً: ٤٣ مسألة
بتاريخ واحد، فظهر الوسم على الجميع ثم اختفى عن الجميع دفعةً واحدة.

وسم «جديد» سبعة أيام، ويُكتم إذا شمل أكثر من **٥٠٪** من المسائل (`NEW_MAX_SHARE`
في `lib/fiqh-data.ts`) — وسمٌ على كل شيء ليس وسماً.

### المقالة

```
ref, id, chapter?, title, excerpt, relatedRefs[], sections[{id, heading, body}], sources?
```

`heading` فارغ في القسم الافتتاحي. في `body`: السطر الفارغ يفصل فقرة، و`**نص**`
يجعله عريضاً. و`sources` من `works` في `terms.json` لا من كتب المذاهب —
١٢ عملاً الآن (كانت ٩)، وسّعت بصحيحي البخاري ومسلم وزاد المعاد بإذن صريح.

### المفردة

```
id, term, briefDefinition, linguistic, legal,
technical: { hanafi|maliki|shafii|hanbali: { text, sources[] } }
```

بنية جديدة (`prompts/glossary-single.md`) — ثلاثة تعاريف مفردة (لغةً · شرعاً ·
موجز) + تعريف اصطلاحي **مستقل لكل مذهب بمراجعه**، بنفس منطق `rulings` في
المسألة. المفردات الثمان الأولى (`hadath`, `niyyah`, ...) ببنية قديمة أبسط
(`term` + `definition` عام بلا تفريق مذاهب) لم تُرحَّل بعد — `validate_content.py`
يفحص البنيتين معاً بالتمييز بمفتاح `technical`.

### الدليل العقلي

```
id, ref, title, tagline, premises[], quran{verse, ref, url}, conclusion
```

`quran.verse` **نصّ عربي مفرد لا كائن لغات** — الآيات لا تُترجم. و`url` رابط
إلى ترجمة مراجَعة على quran.com.

## الأرقام المرجعية — قاعدة لا تُخرق

`F1…` مسائل · `A1…` أدلة · `M1…` مقالات

**الرقم دائم.** بعد نشره لا يُغيَّر ولا يُعاد استعماله أبداً، لأن الناس
يشاركونه ويحفظونه. لو حُذف عنصر بقي رقمه شاغراً للأبد.

و`id` يُخزَّن في محفوظات الزوّار بـ`localStorage`، فتغييره يمسحها.
أما `chapter` و`seq` و`number` فليست معرّفات، وتعديلها آمن.

المفردات بلا أرقام مرجعية — روابطها `#term-najasah`.

## سير العمل — لا تخالفه

1. **`git fetch` قبل أي ادّعاء عن حالة المستودع.** حصل مراراً أن قلت «ينقصك
   كذا» وأنا أفحص نسخة عمرها ساعات. وإن لم تفحص فقل «لم أتحقّق».
2. اسحب آخر نسخة قبل أي تعديل.
3. عدّل في `data/content/`، ثم `python3 tools/build_data.py`.
4. `python validate_content.py` — يجب `PASSED — 0 errors`.
5. `npx tsc --noEmit` ثم `npx next build` عند تغيير الكود.
   و`prebuild` في `package.json` يجمّع ويدقّق تلقائياً.
6. **قارن كل ملف** مقابل النسخة الحية قبل التسليم.
7. **لا تُدرج ملفاً عدّله المالك بعد نسختك** — ملفات `prompts/_core.md` و
   `issues.md` و`articles.md` و`theology.md` و`learn.md` يحرّرها هو.

المالك على **ويندوز / PowerShell** — لا أوامر لينكس.

## قواعد التسليم

1. **أرشيف zip واحد** لكل تسليم — ولا تُرسل ملفاً منفصلاً بجواره، فالعارض
   يجمعهما في أرشيف ثانٍ فيصير zip داخل zip.
2. **بلا مجلد جامع** — `data/` و`components/` في جذر الأرشيف مباشرة.
3. **`خريطة-الأبواب.md` مع كل إضافة** لباب أو مسألة، محدَّثةً. لا تحكم بنفسك
   أنها «لم تتغيّر».
4. لا تعليم في الخريطة **برقم الصفّ** — الملف فيه أربعة عشر جدولاً، كلٌّ فيه
   صفّ برقم ١٢. حصل أن عُلّمت سبعة فصول في باب البيوع خطأً. التعليم باسم
   الفصل داخل قسمه.

## المدقّق — `validate_content.py`

يفحص: الترجمات · المذاهب الأربعة (مسائل) والاصطلاحات الأربعة (مفردات بالبنية
الجديدة) · الأحرف التالفة · النصّ الملصوق بلغة خاطئة · المعرّفات المكررة ·
صيغة `ref` · قوائم المراجع وانتماء كل كتاب لمذهبه (للمسائل ومفردات
`technical` معاً) · الأسماء المختصرة · أقواس الآيات وخلوّها من غير العربية ·
تطابق الأوكرانية مع الروسية · علامات الترقيم في العربية.

**وفحوص أُضيفت 2026-09-05، كلّها من أخطاء وقعت فعلاً:**

- **حرف لاتيني أو تركي داخل كلمة سيريلية** (`а/a`, `і/i`, `і/ı`). لا تراه العين
  ويكسر البحث والفرز صامتاً. وقع في `terms.json` فتكاثر في كل ملف استدعاه.
- **فاصلة عليا نائبة عن العين أو الهمزة** في النقل الصوتي (`Sha'ban` بدل
  `Shaʿbān`). يستثني لواحق الإنجليزية، فـ`school’s` و`don't` تمرّان.
- **درجة حكم غير معروفة** في `grade`.

**لا يفحص:** صحة الترجمة · صحة النسبة الفقهية · **انتقال التصحيح بين اللغات**
— ولهذا الأخير أداة مستقلّة، انظر أدناه.

## مواقيت الصلاة بفروق المذاهب — `lib/prayer-times.ts`

عنصر داخل مسائل المواقيت الخمس (`F40` `F41` `F15` `F42` `F43`) يحسب وقت
الصلاة في **موضع القارئ** اليوم، ويعرض العصر مرّتين: عند المالكية والشافعية
والحنابلة، وعند الحنفية، والفرق بالدقائق. وهذا ما لا تعرضه تطبيقات المواقيت.

- الحساب في المتصفّح بلا حزمة خارجية، فلا يمسّ `output: 'export'`.
- الموقع لا يُطلب إلا بنقرة ولا يُرسل إلى أحد. والمسألة تامّة بدونه.
- مطابق لتقويم أم القرى بالدقيقة بعد إثبات **الاحتياط المقرّر** (`PRECAUTION_MINUTES`)
  الذي تضيفه التقاويم عمداً. الفلك تحته صحيح؛ لا تُعدَّل الزوايا لتوافق.
- الخطوط العليا تُعرض «—» لا رقماً مختلقاً: شمال ٤٨° لا ينتهي الشفق صيفاً.
- المسائل المعنية مثبَّتة في `TIMED_ISSUES` داخل `issue-card.tsx` **بالمرجع لا
  بمطابقة النصّ**، وإلا التصقت ساعة بحكمٍ يذكر العصر عرضاً.

## `tools/check_sync.py` — أهمّ أداة بعد المدقّق

```
python3 tools/check_sync.py origin/main
```

يكشف كل حقل **عُدّل عربيّه وبقيت ترجمةٌ من ترجماته كما هي**. وهذا أشيع خطأ في
هذا المستودع وأخفاه: المدقّق لا يراه لأن الحقول موجودة وأبجديتها سليمة، والقارئ
لا يراه لأنه يقرأ لغة واحدة. أول تشغيل له كشف ١٢ حقلاً فاتت أربعة مدقّقين
بشريين ومراجعاً شرعياً — منها `F43` الإسبانية التي بقيت على «الشفق الأبيض» بعد
تصحيحه إلى الأحمر في اللغات الأربع الأخرى.

**يُشغَّل قبل كل رفع.** ويخرج بالرمز 1 عند وجود تنبيه، فيصلح في `pre-commit`.

**تنبيه:** المدقّق يقرأ `data/fiqhData.json` المبني لا ملفات المصدر. فـ
`build_data.py` **قبله دائماً**، وإلا دقّق نسخة قديمة.

## أخطاء تكرّرت ولا يراها المدقّق

**تسرّب الحروف بين الأبجديات — خمس مرات:** كلمة مجرية في نصّ روسي، و«اجتهاد»
عربية في جملة روسية، و«investigا» في إسبانية، و«صحيح» في روسية، و`vs` في
`F13` (**ما زال**). فحص الأبجدية يتأكّد من **وجود** حروف اللغة لا من **غياب**
غيرها. فافحص كل دفعة يدوياً قبل الدمج.

**خلط دليل الإمكان بدليل الحدوث:** «واجب الوجود» في دليل الحدوث الذي ينتهي
إلى «مُحدِث قديم». وقع مرتين.

**«على المعتمد» في النصوص** — المعروض هو المعتمد دائماً، فذكره يوحي بأن غيره
كذلك. يُحذف. **تكرّر حتى في نصوص وصلت «مُدقَّقة خارجياً»** — لا يُعفى نصّ من
هذا الفحص لمجرد أنه جاء بعد مراجعة مزعومة.

**تسرّب لغوي أوكراني بنفس الأبجدية** — لا يُمسكه فحص الأبجدية لأن الحرف
موجود فعلاً في اللغتين، لكنه رسم روسي لا أوكراني: `исламській` بدل
`ісламській` (و/э بدل і)، و`общині` (روسية بحتة) بدل `громаді`. يحتاج
قراءة معنى لا فحص حروف.

**مراجع خارج القائمة — نفس الكتب تتكرر:** `المبسوط` (حنفي) ظهر ثلاث مرات
مختلفة، و`المقنع` (حنبلي) مرة. راجع القائمة المقفلة دائماً قبل القبول، ولا
تفترض أن كتاباً مشهوراً بالضرورة داخلها.

**مراجع خارج القائمة** — القائمة مقفلة لكنها تُوسَّع بكتاب معتمد حقيقي.
٣١ كتاباً الآن. والأسماء المختصرة في `aliases`، وتُوحَّد بـ
`python3 tools/normalize_terms.py --write`.

## سير المحتوى الجديد

المالك يولّد المحتوى بـ Gemini ثم يعطيه لكلود ليحوّله ويدمجه.

`prompts/` فيه:

| | |
|---|---|
| `issue-single.md` | مسألة واحدة بالمذاهب الأربعة |
| `article-single.md` | مقالة مطوّلة ٥–٨ أقسام |
| `glossary-single.md` | مفردة بتعاريفها الثلاثة |
| `translate-single.md` | ترجمة حقول قائمة إلى لغة جديدة |
| `_terms.md` | **مولَّد** بـ`tools/gen_terms.py` — جدول الكتب المقفل |

كلّها تُلصق **بعد** `القواعد-العامة.md`، وبرومت المسألة معه
`ضوابط-المذاهب-الأربعة.md` كذلك. **ولا تكرّر ما فيهما.**

Gemini **لا يولّد** `ref` ولا `id` ولا `seq` ولا `bookId` — يسندها المستودع.

اطلب **٣ إلى ٥ مسائل** لا عشراً — الالتزام أفضل مع الأعداد الأصغر.

## الأدوات

```
tools/build_data.py         يجمّع data/content في fiqhData.json
tools/gen_terms.py          يولّد prompts/_terms.md
tools/normalize_terms.py    يوحّد أسماء الكتب المختصرة
tools/export_missing.py     يُخرج الحقول الناقصة بلغة ما، بصيغة برومت الترجمة
tools/list_existing.py      ذيل البرومتات القديمة
tools/check_sync.py         يكشف ترجمةً بقيت بعد تعديل عربيّها (انظر قسمه أعلاه)
tools/build_terms.py        يبني data/terms.json
```

## لوحة التحكم

`/admin` — صفحة داخل الموقع (`app/admin/page.tsx`)، ليست أداة خارجية.

الدخول برمز GitHub شخصي بصلاحية `Contents: Read and write` وحدها، يُحفظ في
`localStorage`. تبويب علوي **مسائل / مقالات** — كل نوع بمحرّره. اللغة تُبدَّل
مرة واحدة لكل الحقول لا صندوقاً لكل لغة (٥ لغات، تسمياتها كلها بالعربي:
الإنجليزية · الروسية · الإسبانية · الأوكرانية)، والمراجع قائمة منسدلة من
`terms.json` مفلترة بمذهبها (كتب المذاهب للمسائل، `works` للمقالات)، والتحقّق
يجري أثناء الكتابة بنفس قواعد المدقّق.

**المفردات بلا شاشة بعد** — بنيتها الجديدة (تعريف موجز + لغةً + شرعاً + أربعة
اصطلاحية) أعقد من نموذج تكرار بسيط، تحتاج تصميماً مستقلاً.

## معلّقات

- **`F13`**: `vs` بحروف لاتينية في العنوان الروسي
- **`F22` و`F24`**: تعارض الكراهة في المتغيّر بمجاور — يحتاج مختصّاً
- **كتاب `iman`** أُعيد تسميته «أحكام الإيمان» (كان «الإيمان والمدخل») — الاسم
  القديم كان يوحي بعقيدة، ومحتواه فقه مقارن فعلاً. نقله فعلياً لتبويب العقيدة
  مرفوض حالياً: مكوّن العقيدة (`theology-section.tsx`) يعرض شكل الدليل العقلي
  فقط (مقدّمات ← خلاصة)، لا شكل «مذاهب أربعة» — يحتاج واجهة جديدة لم تُطلب بعد
- **الردّة والتكفير**: `F3` (تارك الصلاة) مخفي بـ`draft: true` بانتظار مرجع
  مختصّ في هذا الباب تحديداً. باقي نواقض الإسلام لم تُكتب أصلاً
- **مفردات مُرحَّلة**: الثمان الأولى (`hadath`, `niyyah`, ...) ببنية قديمة
  (تعريف عام بلا مذاهب)، لم تُرحَّل للبنية الجديدة — قرار مؤجَّل، لم يُحسم بعد
- **مفردات الأحكام التكليفية**: الواجب/المستحب/المباح/المكروه/الحرام — خمس
  مفردات مرشَّحة، فيها خلاف حقيقي بين المذاهب (الحنفية وحدهم يفرّقون بين
  الفرض والواجب كرتبتين)، لكنها أصول فقه لا فقه — تنتظر محتوى من المالك
- **دَين الترجمة**: `ui` أولاً — ١١٠ إسباني و١٢٩ أوكراني
- **قاعدة بيانات الزوّار**: Cloudflare D1 + Pages Functions، الدخول بـ Google
  و GitHub. تنتظر نطاقاً — و`localStorage` يبقى مصدر الحقيقة لغير المسجّل
- **النطاق**: `tibyan.com` غير متاح. `.org` مرشَّح
- **«وجه الدلالة»** في الأدلة العقلية — يحتاج حقلاً ومكوّناً و١١ نصاً
- **بحث عالمي**: **موجود فعلاً ولا ينقصه شيء** — `searchAll()` في
  `lib/fiqh-data.ts` يبحث في كل الأقسام (مسائل · عقيدة · مقالات · مفردات ·
  أسئلة) بكل اللغات الخمس معاً، ومربوط في `app-shell.tsx`. لا تقترح Pagefind
  ولا أي فهرسة خارجية — ستكرّر شيئاً موجوداً وتفقد ميزة البحث عبر اللغات

## أسلوب التعامل

- **راجع صحة المحتوى الشرعي مع مختصّ قبل النشر.** النماذج تخطئ في نسبة
  الأقوال للمذاهب بصياغة واثقة. والشكّ يُثبَت في التسليم لا يُبتلع.
- اسأل قبل التنفيذ إن كان القرار يخصّ المحتوى أو الهوية.
- لا تحوّل شيئاً إلى شكل آخر دون إذن.
- **تحقّق بالفحص لا بالذاكرة**: اقرأ الملف، شغّل الأمر، اعرض الناتج.
- المالك يريد إجابات قصيرة. لا تُطِل.

---

## الحالة عند إغلاق الجلسة — 2026-09-18

**المنجز آخر أسبوع:** `F62`–`F111` · `M6` `M7` · ٣٥ مفردة · نظام الأحكام
التكليفية · البحث العام · نطاق البحث · أبواب العقيدة · الوضع الفاتح · وسم
«جديد» · التلميحة في المقالات بمطابقة الجذر السلافي والعربي · تصنيف المقالات
· مواقيت الصلاة بفروق المذاهب · سدّ خمس فجوات في الخطة (الانتقال بين الحالين
· العجز عن الشروط · صفة الغسل · فسخ النكاح · الكسب وما يحلّ منه).

**معلّق:**
- مسألة **أهل الفترة** — عَقَدية لا فقهية، وأعمدة الموقع مذاهب فقهية. تحتاج
  قراراً: مقالة `M`، أم عرض بالمذاهب العقدية؟
- ٥ مسائل ومقالة أرسلها المستخدم **ولم تصل** — الرسائل الطويلة صارت تُحوَّل
  مرفقات فارغة في محادثة طالت جداً. تُعاد في محادثة جديدة.
- أرشيفات لم تُرفع: `tibyan-prayer-times` · `tibyan-article-chapters-v2` ·
  `tibyan-plan-gaps-v3` · `tibyan-F107-F111-M7`.
- خطوات الصلاة لكل مذهب (البند ٦ من دفعة التعديلات).
- `rukn` و`shart` بلا تعريف في المفردات.

**الفصول اليتيمة (مسألة واحدة):** الآنية · الاستنجاء · المسح على الخفّين ·
إزالة النجاسة · سجود السهو · صلاة المسافر · صلاة الجمعة. خرج منها التيمم.

**درسان من هذه الجلسة يُثبَتان:**
- لا `git checkout` على ملف فيه عمل غير مُودَع — محا `core.json` مرّة
  واستُعيد من أرشيف.
- التحقّق على **المخرج المبنيّ** لا على المصدر: قاعدة CSS داخل `@layer base`
  كانت مكتوبة في المخرج ولا تفوز، وسُلّمت مرّتين على أنها تعمل.

---

## الحالة عند إغلاق الجلسة — 2026-09-18 (ب)

`F112`–`F116` و`M8` أُضيفت. **باب الخطبة تمّ 7 من 7**، وأُنشئ `باب أركان العقد`.
وفُتح **باب مقالات خامس «معرفة الله وصفاته»** في `PREFERRED`.

**قرارات ثُبِّتت في هذه الجلسة:**
- **المرجع خارج القائمة يُستبدل لا يُضاف.** `القواعد المثلى` رُدّ، ومراجع `M8`
  اثنان: `مجموع الفتاوى` و`شرح الطحاوية`. وتوسعة القائمة بالبخاري ومسلم كانت
  لأنهما أصلان في كل باب، لا سابقةً لكتابٍ متأخّر في موضوع واحد.
- **باب المقالات يُسمّى بما يتّسع لما بعده** لا بعنوان المقالة الأولى.
- **الآية عربيّةٌ وحدها** في كل اللغات، والموضع (السورة والرقم) بالخمس. لا استثناء.

**معلّق من هذه الدفعة:**
- `F112`–`F116` **بلا `grade` ولا `nature`** عمداً. وأركان العقد أخصّها: الحنفية
  ركنٌ واحد والمالكية والشافعية خمسة والحنابلة ثلاثة، فشارةٌ موحّدة تُظهرهم
  متّفقين في عين ما يختلفون فيه. تنتظر مراجعتك.
- فصول النكاح الباقية أحد عشر فصلاً بقوائمها كاملةً ولا مسألة فيها.

# طريقة التركيب

فُكّ هذا الأرشيف **فوق مجلد المشروع مباشرة**. مجلد `fiqhpedia` بداخله
يطابق بنية مشروعك، فكل ملف يذهب إلى مكانه تلقائياً ويستبدل القديم.

في ويندوز: انقر يمين على الأرشيف ← `Extract Here` ← ثم انسخ محتويات مجلد
`fiqhpedia` الناتج فوق مجلد مشروعك واقبل الاستبدال.

## ما بداخله

| المسار | الحالة |
|---|---|
| `app/page.tsx` | معدّل |
| `app/layout.tsx` | معدّل |
| `lib/fiqh-data.ts` | معدّل |
| `data/fiqhData.json` | معدّل |
| `components/site-header.tsx` | معدّل |
| `components/category-tabs.tsx` | معدّل |
| `components/school-selector-modal.tsx` | معدّل |
| `components/flag.tsx` | جديد |
| `components/country-picker.tsx` | جديد |
| `components/section-tabs.tsx` | جديد |
| `hooks/use-preference.ts` | جديد |
| `public/flags/` | جديد — ٢٨ ملف SVG |
| `validate_content.py` | معدّل |

## بعد الفكّ

```
python validate_content.py
git add .
git commit -m "feat: countries, section tabs, SVG flags, theology proofs"
git push
```

يجب أن يطبع المدقّق `PASSED — 0 errors` قبل الرفع.

## تحقّق سريع

في `git status` يجب أن تظهر ثمانية ملفات معدّلة وأربعة جديدة
(`flag.tsx`, `country-picker.tsx`, `section-tabs.tsx`, `use-preference.ts`)
بالإضافة إلى مجلد `public/flags/`.

لو ظهر عدد أقل، فملف لم يصل مكانه.

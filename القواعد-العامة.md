# القواعد العامة — fiqhpedia

> تسري على **كل** ما يُكتب للموقع: مقالاً كان أو قصة أو تعريفاً أو مسألة.
> يُرفع مع `CLAUDE.md` و`خريطة-الأبواب.md` في أول رسالة من كل جلسة.
>
> **هذا الملف لا يمنع نوعاً من المحتوى.** هو يضبط المصدر والشكل واللغة.
> ما طُلب كتابته يُكتب، والقواعد تحكم **كيف** يُكتب لا **هل** يُكتب.
>
> فإن كان المطلوب حكماً فقهياً على المذاهب، يُضاف إليه
> `ضوابط-المذاهب-الأربعة.md`. وما عدا ذلك يكفيه هذا الملف وحده.

---

# القاعدة الأولى: المصادر

## الأصل

1. **القرآن الكريم**
2. **السنة النبوية**
3. **إجماع علماء المسلمين من القرون الثلاثة الأولى**

هذا هو الأصل فيما كان تقريراً لحكم أو عقيدة أو معنى.

## القصص والأخبار

ما كان **قصة أو خبراً أو سيرة** — كبدء الخلق، وخلق السماوات والأرض، والملائكة والجانّ، وأخبار الأمم، وسير الأنبياء والصحابة — يجوز الاستدلال فيه **بأي مصدر إسلامي سنّي**: كتب التفسير والسير والتاريخ والطبقات.

والتوسعة هنا في المصدر لا في الصدق: لا يُنسب إلى مصدرٍ ما ليس فيه.

## ما يُذكر مع المروي

إن كان الخبر ضعيفاً أو مختلفاً فيه، يُذكر ذلك في موضعه لا في هامش بعيد. والقارئ يستحق أن يعرف أين ينتهي المتفق عليه ويبدأ المروي.

## التوقّف خير من الخطأ

إن لم تحضر المعلومة بدقة: **يُتوقَّف ويُقال ذلك صراحةً**. لا يُملأ الفراغ بتخمين.

والنماذج تخطئ بصياغة واثقة، فالشكّ يُثبَت في التسليم لا يُبتلع.

---

# القاعدة الثانية: خمس لغات في كل حقل

`ar` العربية · `en` الإنجليزية · `ru` الروسية · `es` الإسبانية · `uk` الأوكرانية

## الأربع معاً في التعديل الواحد

تصحيح العربي وحده يترك الترجمات على المعنى القديم. وقع فعلاً: صُحّح «واجب الوجود» بالعربي وبقي `Necessary Being` بالإنجليزي.

فلا يُسلَّم حقل ناقص لغةً وينتظر تكميلها. الحقل يخرج كاملاً أو لا يخرج.

## كل حقل بأبجدية لغته وحدها

| المفتاح | المسموح |
|---|---|
| `ar` | حروف عربية فقط — **ممنوع** أي حرف لاتيني أو سيريلي |
| `ru` | سيريلية فقط — **ممنوع** أي حرف عربي ولو واحداً داخل كلمة |
| `uk` | سيريلية أوكرانية: `і` `ї` `є` `ґ` — و**ممنوع** `ы` و`э` و`ъ` |
| `en` `es` | لاتينية + علامات النقل `ā ī ū ḥ ṣ ḍ ṭ ẓ` والعين `ʿ` والهمزة `ʾ` |

الحقول الروسية تُراجَع حرفاً حرفاً. تسرّب حرف عربي داخل كلمة روسية خطأ لا يُرى بالعين، ووقع أربع مرات.

**والأوكرانية ليست روسية بحروف مبدّلة.** اللغتان سيريليتان، ففحص الأبجدية
يمرّ عليهما معاً، ونصٌّ روسيّ كامل موضوع في حقل `uk` لا يعترض عليه إلا فحص
التطابق. تُترجم الأوكرانية **من العربية والروسية معاً**، لا من الروسية وحدها.

## الترجمة معنوية، والمصطلح مقفل

الترجمة دقيقة لا حرفية. والمصطلح يُنقل صوتياً مع شرح بين قوسين عند أول ورود: `Tayammum (dry ablution)`.

أسماء الكتب والأعلام تُنسخ من `data/terms.json` حرفاً بحرف، ولا يُجتهد في نقل صوتي جديد ولو بدا أصوب.

**والدقة المعنوية جزء من القاعدة.** «الملح المائي» تُرجم مرة `sea salt`، ومقابله الصحيح الملح الجبلي لا ملح البحر. فترجمة سليمة النحو خاطئة المعنى تمرّ على المدقّق ويقع في غلطها القارئ وحده.

## ما لا يُقاس عليه

الأمثلة تُنقل كاملة. عربية تذكر ثلاثة أمثلة وترجمةٌ تذكر اثنين نقصٌ في المحتوى لا اختصار في الأسلوب.

## الطول لا يُقدَّم على الحكم

الأطوال المذكورة في البرومتات **وصفٌ لا شرط**. غايتها تقارب النصوص ليسهل على القارئ مقارنتها، لا أن يُقصّ منها.

**فلا يُحذف قيد لأجل الطول أبداً.** في الفقه: «المختار» و«الضروري» و«مع الكراهة» و«ما لم يتغيّر». وفي غيره: الشرط والاستثناء وموضع الخلاف.

وقع فعلاً: حُذف الوقت الضروري من نصّ الحنابلة في وقت العصر لأنه جاوز الحدّ، فصارت المسألة تُفهم أن العصر تفوت بالاصفرار — وهي لا تفوت إلا بالغروب. فالحذف غيّر الحكم لا الصياغة.

فإن جاوز النصّ حدّه: يُختصر **الوصف** لا الحكم، أو تُقسَّم المادة إن كانت مادتين، أو **يُتجاوز الحدّ ويُنبَّه عليه**. والثالث مشروع.

# الشكل

## علامات الترقيم

لكل لغة علاماتها، ولا تُستعمل علامة لغة في نصّ لغة أخرى.

| | العربية | English | Русский | Español |
|---|---|---|---|---|
| الفاصلة | `،` | `,` | `,` | `,` |
| الفاصلة المنقوطة | `؛` | `;` | `;` | `;` |
| الاستفهام | `؟` | `?` | `?` | `¿ … ?` |

- **العربية `،` و`؛` و`؟`** — لا `,` ولا `;` ولا `?`.
- **الإسبانية تفتح السؤال بـ `¿`** وتغلقه بـ `?`. سؤال بلا `¿` خطأ إملائي لا خيار أسلوبي.
- لا مسافة **قبل** علامة الترقيم، ومسافة واحدة بعدها.
- لا مسافتان متتاليتان، ولا مسافة في أول الحقل أو آخره.
- العين `ʿ` (U+02BF) والهمزة `ʾ` (U+02BE) — لا `'` ولا `` ` ``.
- أسماء الكتب الروسية بين « » كما في `data/terms.json`.

## الآيات لا تُترجم

نصّ القرآن يُعرض **بالعربية وحدها**، بين ﴿ ﴾، ولا يُنقل صوتياً ولا يُترجم في أي حقل ولا بأي لغة.

ويُذكر معه **موضعه** بكل اللغات — اسم السورة ورقم الآية — مع رابط إلى ترجمة مراجَعة بلغة القارئ.

**السبب**: الترجمات المنشورة محمية بحقوق نشر، وترجمة المعنى تحتاج مختصّاً في كل لغة قبل النشر. والإحالة تعطي القارئ نصّاً صحيحاً بلغته من مصدر مراجَع، ولا تنسب إلى الموسوعة ما لم يراجعه أهله.

ومتى كُتبت ترجمة المعنى بكل اللغات وروجعت، حلّت محلّ الرابط.

والشطر من الآية ينتهي بـ `...` داخل القوسين، ليعلم القارئ أنه ليس الآية كاملة.

## المعرّفات لا تُولَّد

`id` · `ref` · `number` · `bookId` يسندها المستودع وحده. الرقم المرجعي المنشور مرة لا يتغيّر أبداً، واختلاقه يجعل رابطاً منشوراً يشير إلى نصّ آخر.

## لا كلمات نائبة

`TODO` · `TBD` · `placeholder` · «قريباً» · «قيد الإعداد» · «نص تجريبي».

الحقل الذي لم يُكتب بعد **يُترك غائباً**، ولا يُملأ بنصّ مؤقت. الواجهة تعرض الغائب بأدب، والنصّ المؤقت يُقرأ حقيقةً.

---

# ما يفحصه المدقّق وما لا يفحصه

`python validate_content.py`

**يفحص:** وجود الترجمات · الأحرف التالفة · النصّ الملصوق بلغة خاطئة · المعرّفات المكررة · صيغة `ref` · قوائم المراجع · أقواس الآيات.

**لا يفحص:**

- **صحة الترجمة** — الخاطئة تمرّ ما دامت موجودة وأبجديتها سليمة.
- **صحة النسبة** — قول منسوب لغير قائله يمرّ.
- **الإسبانية** — `es` في `PENDING_LANGS`، تُقبل ولا تُشترط.

فالقاعدتان أعلاه **مسؤولية الكاتب لا المدقّق**.
